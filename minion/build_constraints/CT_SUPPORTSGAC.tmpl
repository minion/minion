#include "../minion.h"
/* Minion Constraint Solver
   http://minion.sourceforge.net
   
   For Licence Information see file LICENSE.txt 
*/

#include "../constraints/constraint_supportsgac.h"

template<typename VarArray>
AbstractConstraint*
BuildCT_SUPPORTSGAC(StateObj* stateObj, const VarArray& var_array, ConstraintBlob&)
{ return new ShortSupportsGAC<VarArray>(stateObj, var_array); }

BUILD_CT(CT_SUPPORTSGAC, 1)
