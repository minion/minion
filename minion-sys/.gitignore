debug/
target/
# vendor/ holds the copy of Minion that gets published with the crate,
# written by mini-scripts/vendor-minion.sh at release time. During
# development minion-sys reads Minion from ../, so vendor/ is normally
# absent -- when it is present, build.rs prefers it.
vendor/
*.log
