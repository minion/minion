MINION 3
#TEST CHECKONESOL 1
# Negated-bool bootstrap: `!b` inside negativemddc. The forbidden tuple is
# `[1]`, so `!b = 0`, so `b = 1`. Dropping the `!` would force `b = 0`.

**VARIABLES**

BOOL b

**TUPLELIST**

T 1 1
1

**SEARCH**

PRINT [[b]]

**CONSTRAINTS**

negativemddc([!b], T)

**EOF**
